//11 = x + 5 - an arithmetic operation for this lesson



let coffee = 30;

coffee -=10;
let bread = 50

let matches = coffee - bread;

console.log(`the price for the mix and match is ${matches}`);

matches +=50;


document.getElementById(`info`).textContent = (`the price for the mix and match is ${matches}`);